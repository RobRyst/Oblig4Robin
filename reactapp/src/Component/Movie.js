//import { getMovie } from '../Utils/movieService'

const Movie = () => {

/*
    const getData = async () => {
        //Fremtidig for å kunne hente ut actor fra movieservice fra Utils
        const movie = await getMovie([])
        setMovies(movie)
    }

    useEffect(() => {
        getData()
    }, [])

    console.log(movie)


    return (
        <div>
            {movie.map((movies) => (
                <h1 key = {movies._id}>{movies.name.current}</h1>
            ))}
        </div>
    ) 
    */
}

export default Movie;